# PixelComparison 待办

本文只保留仍然有效的工作项。已完成能力统一记录在末尾，不再保留已失效的实现说明。

## P0：正确性与数据安全

- [ ] **持久化对比任务状态**
  当前 `_TASKS` 和 `_RUNNING` 在进程内存中，后端重启会丢失进度；已提交的空 Comparison 还可能被误认为完成结果。应为对比增加 queued/running/done/error 状态，并处理启动恢复。

- [ ] **强制重算失败时保留旧结果**
  当前重算会先删除旧明细和热力图，最终失败时旧的有效结果也会丢失。应在独立暂存区完成新结果，再原子切换。

- [ ] **限制并验证上传图片**
  增加请求字节数、图片格式、尺寸和总像素上限；分块写临时文件，验证后原子发布。

- [ ] **原子化同名截图并发上传**
  防止两个不同内容的同名请求出现数据库提交者与最终磁盘内容不一致。

- [ ] **前端请求 latest-wins**
  为批次、网格、检查点列表和详情请求增加 request token 或 AbortController，阻止旧响应覆盖新筛选状态。

## P1：性能与工程化

- [ ] **列表图二维虚拟化**
  当前渲染完整 `rows × batches` 矩阵。已有 32×12 约 383 个图片请求的实测，应只加载视口附近单元格，并保持拖拽、折叠、吸附热力图列和键盘预览。

- [ ] **建立 CI 门禁**
  至少运行后端 pytest、前端 Vitest 和生产构建；随后再接入隔离数据的浏览器 E2E。

- [ ] **让 Playwright 脚本可移植**
  移除 `C:/WorkSpace/jm` 绝对路径，将依赖、浏览器安装和测试数据写入项目配置。

- [ ] **版本化数据库迁移**
  用 Alembic 或等价机制替代启动时即席重建表和静默吞掉索引创建失败。

- [ ] **前端拆包**
  当前主 JS 包约 928 KB，Vite 有大 chunk 警告。考虑 Arco/vendor 分包和更细粒度路由加载。

- [ ] **规范对比分享 URL**
  对比 ID 和换向状态都应写入 URL；刷新、复制链接和前进/后退应复现同一画面。

## P2：产品与规模化

- [ ] **CI 视觉质量门禁**
  定义上传、等待任务、评估 pass/warn/fail、输出结果链接和退出码的稳定协议。

- [ ] **基线晋升与退役流程**
  后端已有模型和 service 逻辑，但缺少受审计的 API、权限和前端入口。

- [ ] **鉴权与权限**
  当前只能用于可信内网。对外或跨团队部署前需要身份、读写角色和删除/设置管理权限。

- [ ] **独立任务队列**
  规模扩大后迁移到持久化队列，并提供可恢复任务和 WebSocket/SSE 进度。

- [ ] **PostgreSQL 与对象存储**
  SQLite 和本地文件适合当前规模；更大并发或多实例部署时迁移数据库与图片存储。

- [ ] **滑窗 SSIM 或图像对齐**
  当前是全局简化 SSIM，且分辨率不同会缩放基线图。可评估 windowed SSIM、OpenCV 加速和对齐后比较。

- [ ] **异盘/对象存储灾备**
  当前自动备份与主库默认同盘，只能防误删和逻辑损坏。增加备份目录到异盘或对象存储的同步与恢复演练。

## 已完成

- [x] Vue 3 批次管理、对比结果和项目设置页面。
- [x] 批次列表与“左旧右新”二维列表图。
- [x] 缩略图懒生成、浏览器缓存和 60 天未访问淘汰。
- [x] 列表图角色选择、热力图固定列、拖拽和键盘大图浏览。
- [x] 异步比较、进度轮询、结果缓存和正反向复用。
- [x] 同号批次覆盖、批次删除和级联文件清理。
- [x] 14 天对比结果保留策略。
- [x] SQLite WAL、写锁等待和并发回归测试。
- [x] 数据库、图片和数据根目录可独立配置。
- [x] 每日 SQLite 在线备份、完整性检查和默认 30 天保留。
- [x] 后端 pytest 测试和前端 Vitest 基线。
- [x] 热力图增强参数可从设置页完整保存。
- [x] 缩略图源路径与缓存路径目录边界保护。

# PixelComparison

PixelComparison 管理场景视觉批次及其烘培数据，用于追踪图像差异和资源规模变化。

## Language

**场景**:
截图与烘培数据所属的 UE Level 或 World，是截图跨批次配对和烘培趋势查询的范围；一个场景可以包含多个检查点。
_Avoid_: 检查点、截图名称、GPM 地图配置

**检查点**:
以稳定名称标识的一处截图采样位置或时刻；同一名称用于对齐不同批次、不同画质运行中的截图，帧序和相机信息不是配对身份。
_Avoid_: 场景、截图文件路径、帧序号

**截图批次**:
一次属于单一分支、单一场景和单一平台的采集记录，批次号在全部分支之间全局唯一；批次可包含零个或多个画质运行，以及零或一份烘培数据快照。
_Avoid_: GPMHeatmap 批次、流水线任务

**画质运行**:
同一批次内针对一个唯一 `shading_quality` 产生的截图集合；`shading_quality` 是用户筛选和对比时的业务画质，`tex_quality` 只是保留的采集参数。
_Avoid_: 画质批次、截图组、档位数据

**部分完成批次**:
结构完整的采集包在上传阶段有部分文件未成功写入，但其他完整画质运行或烘培数据仍然可用的批次；采集阶段部分失败不生成可上报批次。
_Avoid_: 失败批次、无效批次

**分支**:
批次数据来源的规范化小写代码分支标签，例如 `main` 或 `engine-ue5`；同一批次只属于一个不可变分支。
_Avoid_: 平台、数据类型

**仅烘培批次**:
包含烘培数据但不包含任何完整画质运行的批次，仍可在批次管理中查询和进入烘培数据页面，但不参与图片预览、基线或图片对比。
_Avoid_: 空批次、无效批次

**数据能力**:
批次实际具备的可用数据集合；至少一个完整画质运行构成截图能力，烘培数据为独立能力。
_Avoid_: 数据类型、批次类型

**待补传批次**:
已经取得全局批次号但尚未具备截图或烘培数据能力的批次，用于保留失败上报的诊断记录，不参与任何图片或烘培数据展示；重报同一批次号必须显式覆盖。
_Avoid_: 空批次、失败批次

**批次管理**:
查询和维护全部截图批次记录的表格目录；它展示各批次的数据能力，但不承担基线选择或截图对比。
_Avoid_: 批次列表图、截图对比

**截图对比**:
同一分支、同一场景内按检查点和画质运行排列截图的对比工作区；用户在此选择同一平台、同一业务画质的基线与对比项、发起对比并查看差异热力图。
_Avoid_: 列表图、对比结果页面

**对比基线**:
一次截图对比中由用户选择的参照画质运行，必须与当前侧属于同一分支、场景、平台和业务画质；它不要求事先成为已认可的基线版本。
_Avoid_: 晋升基线、固定最新批次、基线版本

**晋升基线**:
被认可并赋予版本名称的截图画质运行，在所属分支、场景、平台和画质范围内管理生效或退役状态。
_Avoid_: 对比基线、任意参照侧

**对比记录**:
两个属于同一分支、场景、平台和业务画质且完整可用的画质运行执行对比后持久化的结果数据；它可被截图对比工作区复用，但不对应独立的前端历史或详情页面。
_Avoid_: 对比历史页面、对比结果页面

**差异热力图**:
两个完整画质运行的同名检查点经过图片比较后生成的差异可视化，表达两张截图之间的像素变化。
_Avoid_: GPMHeatmap、性能点位图、烘培分块图

**自动对比候选画质运行**:
与目标画质运行属于相同分支、场景、平台和 `shading_quality`，且双方均完整可用的历史画质运行；手动对比同样不允许跨画质。
_Avoid_: 自动对比候选批次、同场景批次、相邻批次

**主分块**:
场景烘培 Registry 树的根节点，代表不归属于任一下级分块的根 BuildData。
_Avoid_: 总场景、根 Registry

**烘培数据快照**:
一次截图批次随附的场景烘培资源规模记录，包含场景汇总和各 Registry 节点的数据；它属于批次整体，不属于任何画质运行。
_Avoid_: 画质烘培数据、GPM 性能采样、运行时显存实测

**自身数据**:
直接归属于当前选中节点的烘培数据，不包含任何下级节点。
_Avoid_: 节点总数据

**含子级汇总**:
当前节点自身数据与其全部后代节点数据的合计。
_Avoid_: 子块总数据、子级合计

**反射分块**:
主分块下路径以 `_BlockRefl` 结尾的直属 Registry 节点。它没有数字 `blockIndex` 和子分块网格，但作为独立节点展示、查看细则并按 Registry 路径查询趋势。
_Avoid_: 分块 4、主分块自身数据

**GPMHeatmap**:
独立于截图批次、画质运行、烘培数据和截图差异热力图的数据域；后端、接口与文档使用 `GPMHeatmap`，前端顶部入口为简洁的“热力图”，不与对比记录生成的差异热力图混用。
_Avoid_: 点位热力图模块、截图热力图、差异热力图

**GPMHeatmap 批次**:
一次完整、可独立查询、覆盖和删除的 GPMHeatmap 上报，可包含一个或多个场景的点位数据及与点位匹配的截图。
_Avoid_: GPMHeatmap 采集、截图批次

**GPM 点位**:
一张地图在某次 GPMHeatmap 批次中的采样记录，包含序号、位置、方向、指标、详情和匹配截图。
_Avoid_: 截图检查点、地图配置、跨批次点位身份

**跨批次点位身份**:
采集端为同一逻辑采样点持续提供的稳定标识，用于在同分支、地图、平台和画质范围内关联不同批次的单点趋势。
_Avoid_: 点位序号、截图文件名、数据库点位编号、坐标相近

**整体平均趋势**:
按 GPMHeatmap 批次展示采集端提供的地图指标平均汇总；它不由页面当前可见点位重新计算。
_Avoid_: 所选点位趋势、当前色阶平均值、前端计算平均值

**批次号**:
由流水线通过 `pipeline_data.batch_id` 分配，后端将其直接作为截图批次或 GPMHeatmap 批次的 `batch_id`；它在所属数据域内唯一并控制幂等、覆盖和删除。
_Avoid_: client_upload_id、顶层重复 batch_id、全局跨域批次号

**流水线元数据**:
`pipeline_data` 中随批次号一同传入的任务链接、采集时间和分支信息；其中 `batch_id` 创建数据批次，其余字段用于追溯来源和筛选。
_Avoid_: 独立数据批次、上报 manifest

**分段标尺**:
由名称和一组可排序的颜色区间表达式组成的 GPMHeatmap 判定规则；列表顺序用于组织展示，数值归属只由表达式决定，所有区间共同无缝覆盖完整数轴且互不重叠。
_Avoid_: 判定方向、独立阈值、渐变色、动态范围、告警线、固定五色标尺

**指标标尺**:
一个不绑定具体指标、平台、画质或地图的可复用命名分段标尺；指标标尺集通过上报指标 Key 引用它。
_Avoid_: 阈值副本、地图指标配置、指标专属标尺

**指标标尺集**:
由名称及一组用户手工定义的“上报指标 Key → 指标标尺”映射组成的可复用配置；它不从历史上报生成 Key 候选，不预设角色 DC、全部面数等固定指标集合，只有与上报 Key 精确匹配的条目才提供固定分段颜色。
_Avoid_: 标尺方案、指标模板、固定指标清单、平台标尺

**地图标尺绑定**:
一个地图在指定平台和画质下对某个指标标尺集的引用；同一指标标尺集可被多个地图作用域复用，不存在地图单指标覆盖。
_Avoid_: 地图标尺覆盖、地图方案副本、全局标尺集

**动态线性着色**:
当地图的当前平台和画质没有绑定指标标尺集，或指标标尺集没有匹配上报指标 Key 时，依据当前数据集的最小值到最大值生成绿色到红色的连续相对颜色；它只表达批次内相对高低，不表达是否符合预期。
_Avoid_: 默认标尺、自动阈值、合规等级

**地图资源配置**:
地图的名称、排序、描述、坐标起点与范围、轴向和可选底图图片；它不包含平台、画质或指标标尺关联。
_Avoid_: 地图标尺配置、完整地图配置

**标尺配置**:
指标标尺、指标标尺集，以及地图在平台和画质下的指标标尺集绑定；它不包含地图图片或坐标范围。
_Avoid_: 地图资源、地图图片配置

**热力图配置包**:
用于迁移和人工维护 GPMHeatmap 配置的可读 ZIP 包，可只包含地图资源配置、只包含标尺配置，或同时包含二者；不包含批次、点位、截图或趋势数据。
_Avoid_: 数据库备份、上报包、全量数据归档、完整备份

**导入检查**:
解析并校验热力图配置包、计算其相对当前配置的新增与更新，但不修改线上配置的只读步骤。
_Avoid_: 试导入、预应用

**应用导入**:
把已经通过导入检查的配置包应用到当前配置；包外的地图、标尺和标尺集继续保留，包内的指标条目和地图绑定清单表达相应对象的完整配置。
_Avoid_: 覆盖导入、全量替换、直接导入

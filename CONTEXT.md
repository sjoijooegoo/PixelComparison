# PixelComparison

PixelComparison 管理场景视觉批次及其烘培数据，用于追踪图像差异和资源规模变化。

## Language

**批次**:
一次属于单一分支和单一场景的采集记录，批次号在全部分支之间全局唯一；批次可以包含截图、烘培数据或两者。
_Avoid_: 截图批次、流水线批次

**分支**:
批次数据来源的规范化小写代码分支标签，例如 `main` 或 `engine-ue5`；同一批次只属于一个不可变分支。
_Avoid_: 平台、数据类型

**仅烘培批次**:
包含烘培数据但不包含任何截图的批次，仍可在批次管理中查询和进入烘培数据页面，但不参与图片预览、基线或图片对比。
_Avoid_: 空批次、无效批次

**数据能力**:
批次实际具备的可用数据集合；截图与烘培数据是两个可同时存在、也可独立存在的能力。
_Avoid_: 数据类型、批次类型

**待补传批次**:
已经取得全局批次号但尚未具备截图或烘培数据能力的批次，用于保留失败上报的重试入口，不参与任何图片或烘培数据展示。
_Avoid_: 空批次、失败批次

**批次管理**:
查询和维护全部批次记录的表格目录；它展示各批次的数据能力，但不承担基线选择或截图对比。
_Avoid_: 批次列表图、截图对比

**截图对比**:
同一分支、同一场景内按检查点和批次排列截图的对比工作区；用户在此选择基线与对比批次、发起对比并查看差异热力图。
_Avoid_: 列表图、对比结果页面

**对比记录**:
两个具有截图能力的批次执行对比后持久化的结果数据；它可被截图对比工作区复用，但不对应独立的前端历史或详情页面。
_Avoid_: 对比历史页面、对比结果页面

**自动对比候选批次**:
与目标批次属于相同分支、场景、平台和画质，且双方均具备截图能力的历史批次；手动对比只要求同分支、同场景且双方有截图。
_Avoid_: 同场景批次、相邻批次

**主分块**:
场景烘培 Registry 树的根节点，代表不归属于任一下级分块的根 BuildData。
_Avoid_: 总场景、根 Registry

**自身数据**:
直接归属于当前选中节点的烘培数据，不包含任何下级节点。
_Avoid_: 节点总数据

**含子级汇总**:
当前节点自身数据与其全部后代节点数据的合计。
_Avoid_: 子块总数据、子级合计

**反射分块**:
主分块下路径以 `_BlockRefl` 结尾的直属 Registry 节点。它没有数字 `blockIndex` 和子分块网格，但作为独立节点展示、查看细则并按 Registry 路径查询趋势。
_Avoid_: 分块 4、主分块自身数据

# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

> **Important — working-tree state**: These plans were written against `HEAD`
> commit `7621d9e` **plus uncommitted working-tree changes** present at planning
> time (a single-port production deploy in `backend/app/main.py` + `run-prod.ps1`,
> and a batch-preview feature `frontend/src/components/BatchPreview.vue`). The
> "Current state" excerpts in each plan reflect that **live working tree**, not
> the bare commit. Before starting, run each plan's drift check; if the working
> tree was committed or changed, re-confirm the excerpts.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a backend test baseline (pytest + TestClient) | P1 | M | — | TODO |
| 002 | Stop `_TASKS` from growing unbounded (prune finished tasks) | P1 | S | 001 | TODO |
| 003 | Validate uploads (size cap + PNG signature) | P2 | S-M | 001 | TODO |
| 004 | Clean up orphaned image/heatmap files | P2 | M | 001 | TODO |
| 005 | Server-side pagination for `/api/batches` | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 must land first.** It creates the pytest harness (`backend/tests/`,
  `conftest.py`, dev deps) that 002–005 each extend with a regression test.
  Without it, those plans have nowhere to put their tests and no way to verify
  they didn't break the request/compare flow.
- 002–005 are independent of each other and may be done in any order after 001.
- 005 is the only one that changes an API response shape and touches the
  frontend; do it last so the smaller backend-only fixes are already verified.

## Findings selected for planning

From the 2026-06-17 audit, the maintainer selected findings #1, #2, #4, #6, #7
→ plans 001, 002, 003, 005, 004 respectively.

## Findings considered and NOT planned (this round)

These were surfaced in the audit but not selected for plans yet — listed so they
aren't re-audited from scratch:

- **N+1 queries in list endpoints** (`main.py` `batch_dto`/`comparison_dto`/
  `list_baselines`): per-row COUNT subqueries. Real but deferred.
- **Global mutable settings bleed across users** (`settings.py`): one user's
  threshold change alters everyone's results. Deferred (architectural).
- **In-memory task state blocks multi-worker** (`_TASKS`/`_RUNNING`): documented
  production constraint, requires moving task state to the DB. Deferred.
- **No managed DELETE endpoints / no auth / unpinned deps / no linter+CI**:
  deferred.
- **Baseline promotion has no API endpoint** (`promote_baseline` only called by
  `seed.py`): half-built feature; needs a product decision, not just a fix.

## Rejected (by design — do not re-report)

- CORS `allow_origin_regex=".*"` + no auth (`main.py`): deliberate choice for an
  internal LAN tool with open access. Settled.
</content>
</invoke>
